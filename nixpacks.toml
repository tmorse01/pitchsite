[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = [
    'npm ci',
    'cd server && npm ci',
    'cd client && npm ci'
]

[phases.build]
cmds = [
    'cd server && npm run build',
    'cd client && npm run build'
]

[start]
cmd = 'cd server && npm start'
